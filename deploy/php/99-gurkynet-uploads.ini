; GurkyNet PHP-FPM upload / request limits
; Install as: /etc/php/8.4/fpm/conf.d/99-gurkynet-uploads.ini
; Affects php-fpm ONLY (not CLI unless also linked under cli/conf.d)

upload_max_filesize = 20M
post_max_size = 25M
max_execution_time = 300
max_input_time = 300
memory_limit = 256M
