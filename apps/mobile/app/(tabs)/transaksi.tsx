import { ScreenContainer, ComingSoon } from '../../src/components/ui';

export default function TransaksiScreen() {
  return (
    <ScreenContainer scroll={false}>
      <ComingSoon icon="grid" title="Katalog Produk" phase="Fase 3 (Katalog & Checkout PPOB)" />
    </ScreenContainer>
  );
}
